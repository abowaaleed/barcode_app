name: barcode_app
description: Smart QR/Barcode Generator - Professional QR code and barcode creation tool
version: 1.3.1+5

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.6
  qr_flutter: ^4.1.0
  barcode: ^2.2.9
  provider: ^6.1.1
  intl: ^0.20.2
  share_plus: ^7.2.2
  url_launcher: ^6.2.1
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
